# BusinessPlanner
Business with Purpose

## Firebase Setup

1. Copy `.env.example` to `.env`.
2. Fill in your Firebase web app values.
3. Create a Firestore database in your Firebase project.
4. Run `npm start` for local development.

If Firebase env values are missing, the app falls back to browser `localStorage`.
